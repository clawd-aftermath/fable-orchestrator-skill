# Standing project brief — <PROJECT NAME> (updated <DATE> by <who maintains this>)

**Product goal:** <one sentence: what the project must deliver, for whom. Note any
past scope changes so the advisor can flag drift.>

**Canonical repo/workspace:** <absolute path. Name any directories that must NOT
be used or written to.>

**Current state (<DATE>):** <2-4 sentences: what just happened, what was decided,
what is explicitly rejected/closed. Update whenever direction changes — the
advisor re-reads this file on every call.>

**Active direction:** <the currently authorized next experiment/milestone and its
success criterion.>

**Standing red flags for any advice you give:**
- <project-specific trap #1 — e.g. "component labels inflate: verify solver/
  independent/frozen/validated claims against files">
- <trap #2 — e.g. "every metric/gate must be falsifiable: check the code that
  computes it can actually fail">
- <trap #3 — e.g. "experiments must derisk the measured failure, not an easy
  adjacent slice">
- <governance cap — e.g. "one frozen one-page spec per experiment, then execute;
  push back on spec/seal chains">
- <approval boundaries — e.g. "sustained runs / paid compute / promotions need
  explicit user approval, recorded in-repo at the time">
